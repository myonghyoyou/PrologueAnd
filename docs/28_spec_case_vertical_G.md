# SPEC — Case Study 상세 (세로, G안) + Projects 전면 페이지

| 작성일 | 2026-09-22 |
|---|---|
| 결정 | **G 확정**(2026-09-22): C의 한 격자·전폭 화면 표지 + E의 판 리듬. 07은 스크롤 와이프. 헤더 진행 지도·`NN / 10` 없음. 실물 시안 `design/spec/09-case-midfi.html#G` |
| 전제 | 전면 페이지(docs/23 B). 시트 삭제, 헤더 통일, 목록 승격은 `cae88f4`에서 되살림. 10장 데이터는 docs/25 §4 스키마(`v3/cases.js`) |
| 검증 | 페이지의 `window.__spec()` + 수동 |

## 1. 헤더·목록·대시보드 (docs/26 H·L·D 그대로)
H1~H5, L1~L5, D1~D4 동일. 단 **상세 헤더는 `Prologue& · 제목 / ← Projects / 문의`만** (H4에서 `NN / 10` 삭제, 스트립 없음).

## 2. 상세 배치 (G)
| ID | 요구 | 검증 |
|---|---|---|
| G1 | 한 격자: 왼쪽 시작선 112(헤더 브랜드), 내용 폭 최대 1120(`.wrap`). 그림 폭 = 격자 폭, 1280 원본보다 키우지 않음 | 자동: 표지 화면·판·전폭 그림의 left 동일, 화면 `--sc ≤ 1` |
| G2 | 표지: 전폭 라이브 화면 + 캡션 → 제목 56px + 메타 한 줄 \| 오른쪽 숫자 3개(밑변 맞춤) | 자동: 요소 존재·순서 |
| G3 | **모든 판 = 화면 한 장**(100vh − 64, 하한 640). 글(번호 라벨 + 제목 48px + 문단 18px)은 위, 그림은 남는 높이를 다 쓴다(fitPans). 08은 숫자 160px, 마지막 판은 09 \| 10 + 다음 이야기 티저. 괘선 없음 (2026-09-22 사용자: 판마다 꽉 차게) | 자동: `.pan` 8개 높이 = vh − 64, 넘침 0, 그림 판 채움률 ≥ 75% |
| G4 | 그림 폭 = min(격자 폭, 남는 높이 × 비율). 표지 화면은 판 높이의 56% 이하로 제목·숫자와 한 장에 | 자동: 표지 넘침 0 |
| G5 | 03·04·05는 한 판(큰 문장 제목) + 다이어그램이 **한 덩어리로 화면에 붙고**(sticky, 120vh 머무름), 구간을 지나는 동안 t 0→1 morph. 애니메이션이 끝나기 전에는 아래로 내려가지 않는다(2026-09-22 사용자) | 자동: 구간 t, `.g-stick` 높이 + 64 ≤ vh |
| G6 | 07 와이프: 판 + 두 화면이 한 덩어리로 화면에 붙고(sticky, 120vh), 경계선 왼→오른쪽. 끝나야 내려간다. Before 화면 없는 프로젝트는 After 한 장 정적 | 자동: t 0/.5/1에서 clip-path, `.g-stick` 높이 + 64 ≤ vh |
| G7 | 08: 숫자 3개 88px + 전·후 값 있는 막대(화면에 들어오면 0.6s) | 자동 |
| G8 | 09 \| 10 한 판 좌우. 10에 문의 CTA(`data-project`) + 메일. 끝은 다음 이야기 티저 + 정적 푸터 행 | 자동 |
| G9 | 06 핫스팟: 캡션 ≤3 호버(탭) → 화면 영역 강조 + 배지 | 자동 |
| G10 | 다른 slug는 같은 본문에 제목·캡션만 교체(docs/25 전제). `next`는 자기 자신이 아님 | 자동 |

## 3. 동작
| ID | 요구 | 검증 |
|---|---|---|
| B1 | Lenis 세로(lerp .1). `html{overflow-x:clip} body{overflow:visible}` — body overflow-x:hidden은 sticky를 깨뜨림 | 자동: computed |
| B2 | 키 ↓/→/PgDn/Space = 다음 판 시작, ↑/←/PgUp = 이전, Home/End | 자동 |
| B3 | 해시 `#s07`로 진입하면 그 판으로 즉시 | 자동 |
| B4 | `← Projects`: 목록에서 왔으면 history.back(), 아니면 projects.html. 브랜드 → `../v6/index.html#p06` | 수동 |
| B5 | 목록 → 상세 제목 공유 VT(`pj-title`), 상세 → 상세는 root 페이드(제목 morph 없음) | 수동 |
| B6 | 문의 서랍이 세 페이지에서 같은 동작, 상세에서는 프로젝트가 붙음 | 자동: `#drawer` |
| M1 | <1024: 한 단, 그림은 글 아래, 와이프는 Before/After 두 장 정적, morph t=1, 가로 넘침 없음 | 자동(390px iframe) |
| A1 | reduced-motion: Lenis 없음, morph·와이프 최종 상태, 막대 즉시 | 수동 |

## 4. 범위 밖
진행 지도·`NN / 10`·번호 레일·선(S1)·`&` 마커·이동 중 축소·Ctrl+F·고객의 말·기술 스택·D9·D10.

## 5. 검증 결과 (2026-09-22, Chrome, `window.__spec()`)
| ID | 결과 | 값 |
|---|---|---|
| H·L·D | 통과 | `cae88f4` 되살림: 헤더 64·hdr VT·메뉴 40px, 목록 밀도, 시트 없음. 상세 헤더에 스트립·`NN / 10` 없음 |
| G1 | 통과 | 표지 화면·판 7·전폭 2 모두 left 386(= wrap 274 + 112), `--sc` 최대 .86 |
| G2·G3·G4 | 통과 | 판 8개 높이 836(900)·656(720)·1016(1080), 넘침 0. 그림 판 채움 76~91%, 08 50~66%. 표지 화면 363(900)·210(720)·530(1080) |
| G5 | 통과 | 머무름 블록 높이 707(900)·642(720). JS 붙이기(translateY)로 구간 t 0/.5/1에서 블록 top 64 유지, 구간 뒤 해제(−410) |
| G6 | 통과 | 머무름 블록 762(900)·582(720), 구간 t 0/.5/1 → 블록 top 64 유지, clip 100%/50%/0%. CSS sticky는 환경에 따라 안 붙어 JS translateY로 교체 |
| G7·G8 | 통과 | 막대 1(4→1), 다음 이야기 Daeryun, CTA `data-project`, 푸터 행 |
| G9 | 통과 | 배지 3, 캡션 2 호버 → `.spot-hl` on |
| G10 | 통과 | `?p=custom-commerce` → 제목·캡션 교체, 예시 표기, next ≠ self |
| B1 | 통과 | html overflow-x clip, body visible, Lenis on |
| B2 | 통과 | ↓ → 07 판 top 89, ↑ → 06 top 88 |
| B3 | 통과 | `#s06` 진입 → top 88 |
| B4 | 통과(흐름) | 대시보드 → 목록 → 상세 → ← Projects(back) → 브랜드 → `v6#p06`(pos 5268) |
| B6 | 통과 | 서랍 존재, 헤더 문의에 프로젝트 |
| M1 | 통과 | 390px: 한 단, 와이프 static·After 정적, scrollWidth 375 ≤ 390 (CTA 줄바꿈·grid min-width:0으로 수정) |
| B5·A1 | 미확인 | 전환 모양·reduced-motion은 사람이 확인 |

