# V6 경로형 설계 — 꼬인 선 하나가 풀리며 장면을 잇는다

| 작성일 | 2026-09-18 |
|---|---|
| 상태 | 설계 초안 — 경로 시안(정적 SVG) 제작 전 검토용 |
| 배경 | 외부 피드백: "복잡한 프로세스가 Prologue&를 만나 바뀐다"를 선 하나로. 인트로는 꼬인 선, 전체 구도는 풀린 선이 굽이치며 내려가는 경로, 카메라가 선을 따라 이동하며 점마다 장면. 캐러셀처럼 움직이면 안 되고 유려해야 함(huyml.co) |
| 유지 | 13 컬러 · 14 타이포 · 21 문구 · 장면 9개 · S1(한 선) S2(&가 선을 탐) S3(빈칸) S4(북엔드) · 시트·서랍 · 점 단위 이동 엔진 |
| 바뀜 | 가로 스트립(17) → **2차원 경로 위 카메라 이동**. `scrollLeft` → 경로 거리 `t` |

---

## 1. 한 문장으로
**꼬인 선 하나가 풀려 나와 아홉 개의 점을 지나 `&`에 닿는다.** 화면은 고정된 창이고, 그 창이 선을 따라 미끄러진다. `&`는 늘 창의 한가운데 있고 세계가 그 아래로 흐른다.

---

## 2. 세계(world) 좌표계
- 세계는 **1440×900 화면을 기준으로 그린 고정 캔버스**(단위 = px). 다른 화면 크기에서는 `scale = min(vw/1440, vh/900)`로 통째로 축소·확대한다. 구도가 화면마다 달라지지 않고, 판·글자·선이 함께 커진다(포스터를 확대하는 것과 같음).
- 세계 크기: 약 **3600 × 5400** (가로 2.5화면 × 세로 6화면). 그림 2의 비율(위에 꼬임, 아래로 굽이침)을 따른다.
- 카메라: `world.style.transform = translate(vw/2 - P(t).x·s, vh/2 - P(t).y·s) scale(s)`. 회전 없음(글자는 항상 수평).

## 3. 경로(path)

### 3.1 형태
1. **꼬임(0장면)**: 세계 상단 중앙, 약 700×420 상자 안의 스크리블(고리 6~8개, 3~4번 교차). 선의 시작은 상자 왼쪽 위 밖에서 들어온다(그림 1의 왼쪽 위 점).
2. **풀림**: 꼬임 오른쪽 아래로 한 가닥이 빠져나와 첫 큰 굽이로.
3. **굽이 6개**: 좌↔우로 크게 왕복하며 내려간다. 진폭 1200~1800px, 한 왕복 높이 900~1100px. S자마다 곡률이 다르게(단조로움 방지).
4. **끝**: 마지막 굽이가 짧아지며 수평에 가깝게 들어와 `— & —` 자리(세계 하단 중앙)에서 멈춘다. 오른쪽 가로선 없음(현행과 같음).

### 3.2 수학
- 하나의 SVG `<path>`, 3차 베지어 연속(C1: 이음점에서 접선 방향·크기 일치).
- 길이 매개변수 `t ∈ [0, L]` (`getTotalLength`), 위치 `P(t) = getPointAtLength(t)`. 접선은 `P(t+1) − P(t)`.
- **점(장면) = 특정 t**: `T = [t0 … t8]`. 스냅·진행 지도·해시는 전부 이 배열.

### 3.3 점 좌표 (2026-09-18 경로 시안 `design/spec/07-path.html`로 확정)
세계 **2800 × 5100**. 검증: 모든 점에서 다음 점이 화면 1.2배 안에 보임, 판 겹침 0, 선 관통 0, 점 사이 경로 길이 618~1050(무엇을 안 276·348). 총 길이 7,758.

| # | 장면 | 위치 (x, y) | t | 판 |
|---|---|---|---|---|
| 0 | Prologue | 꼬임 상자 1150~1850 × 310~730, 출구 (1780, 700) | 0 | 없음. 헤드라인은 꼬임 왼쪽 |
| 1 | 처음 | (2200, 1100) | 618 | 오른쪽 위 |
| 2 | & | (1300, 1600) | 1662 | 왼쪽 위 |
| 3 | 끝까지 | (2000, 2050) | 2532 | 오른쪽 |
| 4 | 회사든, 개인이든 | (1150, 2550) | 3534 | 왼쪽 |
| 5 | 무엇을 | (1700, 3000) · (1950, 3120) · (2200, 3240) | 4278 · 4554 · 4902 | 세 점 위쪽, 중심 (1950, 2760) — 직접 지정 |
| 6 | Projects | (1300, 3750) | 5952 | 왼쪽 아래 |
| 7 | 문의 | (2100, 4250) | 6924 | 오른쪽, navy |
| 8 | — & — | (1500, 4800) | 7758 | 없음 |

- 판 크기 **700 × 420**, 점에서 곡선 바깥쪽으로 56px. 점이 여럿인 장면만 판 자리를 직접 지정.
- 처음 점(618)이 가장 짧다 — 인트로에서 꼬임을 풀고 나오는 첫걸음이라 짧아도 됨.
- 확인된 결정(2026-09-18): 판 유지 / 카메라 회전 없음 / 이동 중 축소는 토글로 / 세로 스크롤바 없음.

## 4. 장면(판)
- 판 = 14 규칙 그대로(bone-0, 1px bone-200, 2px, 그림자 없음). 크기 **760×460** 기준(포스터 헤드라인 2줄 7.8rem → 세계 기준 100px 급).
- **판의 위치**: 점에서 접선의 바깥쪽(곡선의 볼록한 쪽)으로 60px 떨어져 붙는다. 점 자체는 선 위에 남고 `&`가 도착하면 그 위에 선다(현행과 같음).
- 등장: 카메라가 점에 접근할 때(`|t − ti| < 500`) 판이 8px 떠오르며 나타나고(250ms), 지나간 뒤에도 남아 있다(뒤를 돌아보면 있음). 등장 애니메이션은 이 한 번뿐(14 §4).
- 판 내용: 지금 v5의 9장 그대로(21 문구). 문의 판만 navy-950.
- 판이 없는 점(0, 8): 헤드라인·캡션이 세계에 직접 놓인다(포스터 위의 글자).

## 5. 카메라와 입력
| 항목 | 규칙 |
|---|---|
| 상태 | `t`(목표) / `tPos`(현재), smoothDamp 0.26s — 현행 엔진 그대로 |
| 휠 노치 / 방향키 | 다음·이전 점의 `ti`로 목표 갱신(현행 규칙: 스트림 하나 = 점 하나, 큐) |
| 속도 상한 | 세계 기준 **2400px/s**. 넘으면 smoothTime을 늘려 감속 |
| 곡률 감속 | 접선 방향 변화율이 큰 구간(굽이 정점)에서 목표 속도 ×0.7 — 카메라가 코너에서 자연스럽게 느려짐 |
| 앞보기(lookahead) | 카메라 중심을 `P(t)`가 아니라 `P(t + 120·v/vmax)`로 — 빠를수록 앞을 본다. 정지 시 0 |
| `&` 마커 | 화면 중앙 고정(선 위 20px). 세계가 움직인다. 점에 서면 점 위 |
| 잉크 | 선은 `t`까지 그려짐(현행). 앞은 옅은 안내선 |
| 진행 지도 | 점 9개(무엇을은 1칸에 점 3개). 현재 = `&`가 있는 점 |
| 해시 | `#p0n` 동기화, 직접 진입 시 인트로 생략 |
| 이동 중 축소 | 유지 가능(세계 scale에 곱함). 토글로 |

## 6. 인트로 (0 → 1)
1. 로드: 꼬임이 1.4초 동안 그려진다(stroke-dashoffset). 헤드라인은 즉시.
2. `&`가 꼬임 끝(풀리는 지점)에 놓인다(페이드 0.4초).
3. 첫 입력에서 카메라가 풀린 가닥을 따라 점 1로 미끄러진다. 이때만 카메라 속도를 0.8배로 — "풀리는" 느낌.
4. 뒤로 돌아오면 꼬임이 그대로 있다(다시 그리지 않음).

## 7. 끝 (7 → 8)
- 점 8에 도착하면 `&`가 자리(seat)에 앉고, 위 `Prologue`, 아래 캡션·링크가 나타난다(현행). 선은 자리 34px 앞에서 끝난다.
- 더 굴려도 움직이지 않음(끝). 진행 지도 마지막 칸.

## 8. 모바일(≤1023px)·reduced-motion
- 모바일: 경로 없음. 9장면 세로 스택(현행 그대로).
- reduced-motion: 카메라 이동은 즉시 점프(smoothTime 0), 등장·잉크 없음.

## 9. 구현 메모 (V5에서 옮기는 것)
| V5 | V6 |
|---|---|
| `wrap.scrollLeft` | `t` (경로 길이) |
| `snaps` (scrollLeft 값) | `T` (t 값) |
| `htrack` translateX | `world` translate(−P(t)·s + 화면중심) scale(s) |
| `#hline` 직선 | 세계 안의 곡선 `<path>` 하나 + 잉크 복제 |
| 판 `.hp` flex 나열 | 판 `position:absolute; left/top = 점 + 오프셋` |
| `render()`의 tipX | `t`; `&`는 고정 |
| 어두운 구간 클리핑 | 문의 판만 어두우므로 판 안에서 선 색만 흰색(판이 선 뒤에 있으면 불필요) |

산출물: `design/spec/07-path.html`(경로 시안: 세계 전체를 한 화면에 축소, 점·판 상자·다음 점 가시 범위 표시) → 확인 후 `design/mockups/v6/`.

## 10. 수용 기준
| # | 검사 | 기준 |
|---|---|---|
| a | 점 9개에서 정지 시 `&`와 점 좌표 | ≤ 2px |
| b | 판 겹침 | 판 상자끼리, 판과 선(자기 점 앞뒤 200px 제외) 0건 |
| c | 다음 점 가시성 | 모든 점에서 다음 점이 화면 1.2배 안 |
| d | 속도 | 어떤 구간도 2400px/s(세계) 초과 없음 |
| e | 휠 노치 하나 = 점 하나, 큐, 방향키 | 현행 검증 반복 |
| f | 1280 / 1536 / 1920 / 2560 | 구도 동일(scale만 다름), 판 글자 겹침 없음 |
| g | 모바일 400 | 세로 스택, 가로 넘침 없음 |
| h | reduced-motion | 즉시 점프, 애니메이션 없음 |

## 11. 결정 기록
- 2026-09-18 — 초안. 검토(§12) 후 경로 시안 제작.
- 2026-09-18 — 사용자 확인: 판 유지, 회전 없음, 축소는 토글, 스크롤바 없음. 경로 시안 `spec/07-path.html` 검증 통과(§3.3).

---

## 12. 자체 검토 — 빠진 것과 위험

### 심각 (이대로 만들면 잘못 만들어지는 것)
1. **세계 통째 스케일과 글자 크기의 충돌**: `scale = min(vw/1440, vh/900)`이면 1280×720 창에서 0.8배 → 헤드라인 7.8rem(125px)이 100px, 본문 1.2rem(19px)이 15px. 2560×1440에서는 1.6배 → 본문 30px. 14의 타이포 규칙(크기 3단계 고정)과 어긋난다. → **판 안의 글자는 세계 스케일에서 제외**하고(판을 `scale(1/s)`로 되돌린 뒤 판 크기는 CSS px 고정), 경로·점·판의 **위치**만 스케일한다. 판이 화면 대비 같은 비율을 차지하려면 판 크기를 `clamp`로 따로 정한다.
2. **판이 선을 가린다 ↔ 선이 판 뒤를 지난다**의 z 순서와 잉크 표시: 잉크(그려진 선)가 판 뒤로 들어가면 `&`가 판 위에 있을 때 선의 끝이 안 보인다. → 판은 점에서 **접선 바깥쪽**에 두어(§4) 선이 판을 관통하지 않게 하고, 관통이 불가피한 구간은 경로를 고친다. 검증 (b)를 자동 계산으로.
3. **`getPointAtLength`를 매 프레임 여러 번** 호출(카메라·앞보기·곡률·잉크) — 긴 경로에서 프레임당 4~6회는 괜찮지만, 곡률 계산을 매 프레임 하면 비용이 큼. → 로드 시 `t`를 4px 간격으로 샘플링해 위치·접선·곡률 표를 만들어 두고 보간한다.

### 보강 (만들면서 채울 것)
- 문의 판(navy-950)이 세계 위에 놓이면 판 바깥의 선·점은 밝은 배경 위라 색 전환 문제가 사라진다. 대신 어두운 판 **안**을 지나는 선이 있으면 흰색이어야 함 → 판 안에서는 선을 그리지 않는 규칙으로(판이 점 옆에 있으니 자연스럽게 해결).
- "무엇을"의 점 3개: 한 판에 점 3개면 판이 세 점을 모두 덮을 수 있음 → 판을 굽이 위쪽에 두고 점 3개는 판 아래 선 위에. 카메라가 세 점을 지나는 동안 판이 시야에 머무름(카메라가 300px씩만 움직임).
- 뒤로 돌아갈 때 카메라 방향 전환: smoothDamp는 방향 반전에서 속도가 0을 지나므로 자연스럽지만, 앞보기(lookahead)가 부호를 바꾸며 튈 수 있음 → lookahead는 `v`의 부호를 따르되 0 근처에서 0으로 부드럽게.
- 진행 지도의 "칸 폭 ∝ 패널 폭" 규칙은 의미를 잃음 → 점 사이 경로 길이에 비례.
- 해시 진입·새로고침 시 `t`가 점이 아닌 값이면 가장 가까운 점으로.
- 판 등장 상태의 초기값: 첫 화면(점 0·1)은 로드 시 이미 보여야 함("Show the page at rest").

### 확인 필요 (사람에게 물어야 답이 나오는 것)
1. **판의 존재**: 피드백 그림 3은 흰 사각 "판"이 점에 붙어 있다. 우리도 판을 유지할지, 아니면 판 없이 글자·도형이 세계에 직접 놓이는지(더 유려하지만 14의 "지면" 규칙과 어긋남). → 초안은 판 유지, 다만 크기 축소(760×460).
2. **회전**: 카메라가 접선 방향으로 살짝 기울면 더 영화적이지만 글자가 기운다. 초안은 회전 없음.
3. **이동 중 축소** 유지 여부.
4. **세로 스크롤바 노출**: 경로가 아래로 흐르므로 방문자가 세로 스크롤을 기대할 수 있다. 휠은 경로 이동으로 쓰고, 스크롤바는 없음(현행). 그래도 될지.

### 덜어낼 것
- 회전 카메라, 3D(원근) 효과, 판마다 다른 크기 — 이번 범위 밖.
- 곡률 감속·앞보기는 2차(경로 시안 확인 → 엔진 1차 → 리듬 조정).

## 13. 시안(design/mockups/v6)에서 설계와 달라진 것 — 2026-09-18
| 항목 | 설계 | 시안 | 이유 |
|---|---|---|---|
| 판 크기 | 760×460 → 700×420(spec/07) | **640×420** | 점 옆(GAP 56)에 둔 판이 1440 창 가장자리를 60px 넘침. 640이면 점 ±376 안에 들어감 |
| 판 자리 | 접선 바깥쪽 + GAP | 같음 + **기준 창(1440×900, 여백 24, 헤더 아래 84) 안으로 clamp**, 선을 지나면 접선 방향으로 밀어 피함 | 판이 화면 밖·헤더 아래로 나가는 자리를 자동으로 막음 |
| 무엇을 점 3개 | (1700,3000)·(1950,3120)·(2200,3240), 판은 위 | **(1700,3000)·(1700,3200)·(1700,3400) 세로**, 판은 오른쪽 (1756~2396 × 2986~3406) | 점 위에 판을 두려면 카메라 중심 위로 476px가 필요한데 헤더를 빼면 386px뿐. 세로 줄로 바꾸면 카메라가 세 점을 내려가는 동안 판이 계속 시야에 있음 |
| 인트로 카메라 | 꼬임 끝(1780,700) 중심 | **(1240,600) 중심**, 첫 점의 6할 지점까지 오프셋이 0으로 풀림 | 꼬임 끝을 중앙에 두면 헤드라인(꼬임 왼쪽)이 화면 밖. 첫 이동에서 &가 화면 중앙으로 들어오는 것이 "풀림"과도 맞음 |
| 세로 스크롤 | — | `.stage{overflow:clip}`, 0번 장면은 해시 없음 | `#p00` 해시가 overflow:hidden 컨테이너를 스크롤시켜 세계가 178px 밀리는 문제 |
| 선 위 & | — | v5와 같이 선 위 20px, 배경 원 없음 | v5 결정 그대로 |
| 모바일 미리보기 | `?embed=1` | `?mob=1` | embed는 헤더까지 숨김 |
